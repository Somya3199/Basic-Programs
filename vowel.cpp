#include<iostream>
using namespace std;
int main()
{
	char ch;
	cout<<"Enter any character";
	cin>>ch;
	if(ch=='a'||ch=='A')
    cout<<"Entered character is vowel";
    else 
    if(ch=='e'||ch=='E')
    cout<<"Enterd character is a vowel";
    else 
    if(ch=='i'||ch=='I')
    cout<<"Enterd character is a vowel";
    else 
    if(ch=='o'||ch=='O')
    cout<<"Enterd character is a vowel";
    else 
    if(ch=='u'||ch=='U')
    cout<<"Enterd character is a vowel";
    else
    cout<<"Enterd character is a consonant";
    
}
